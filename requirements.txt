# flowintel-reverify — Python dependencies
# Install into the Flowintel venv:
#   /opt/flowintel/env/bin/pip install -r requirements.txt
#
# ssdeep also requires the system library:
#   apt install libfuzzy-dev

reverify>=0.10.0
yara-python>=4.5.0
python-tlsh>=4.5.0
ssdeep>=3.4
pymisp>=2.5.0
requests>=2.28.0
playwright>=1.40.0
